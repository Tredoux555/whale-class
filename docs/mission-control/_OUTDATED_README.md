# ⚠️ These files are months out of date

`brain.json` (last session 115), `mission-control.json` (Jan 25, 2026) and
`SESSION_LOG.md` (session 45, Jan 22, 2026) stopped being maintained.

**The canonical project brain is `/CLAUDE.md` at the repo root**, with
`/HANDOFF_LATEST.md` as the resume-from-here pointer.

Kept for history (archive-never-delete rule, audit run Jun 2026). Do not
trust their contents; do not update them — update CLAUDE.md instead.
