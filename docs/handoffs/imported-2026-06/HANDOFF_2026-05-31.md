# Montree Principal Platform — Session Handoff (2026-05-31)

Context: hardening the **principal cockpit** (`montree.xyz/montree/admin`, school **Tredoux House** `c6280fae-567c-45ed-ad4d-934eae79aabc`, principal "Principal Leu") so it could be onboarded to a real, skeptical principal. All code pushed to `main` (GitHub `Tredoux555/whale-class`); Railway auto-deploys on push.

---

## What was fixed / built (by area)

### 1. Principal page "jumping around" — FIXED ✅ (verified live)
- **Cause:** a redirect loop for a **cookie-only session** (valid auth cookie but no `montree_principal` in localStorage): admin → login-select → (recoverSession finds cookie) /dashboard → (dashboard sends principal) /admin → repeat. Also a pre-auth 401 storm on `/snapshot` + `/billing/status`. NOT stale code / service worker.
- **Fix:** the `/montree/admin` layout is now the single auth gate, and it no longer bails when localStorage is missing — `auth/me` is the authority and self-heals. Commits **f05b070f** (gate) + **4f338814** (loop).
- Verified: clean single redirect, no bouncing, no dashboard 503 spam.

### 2. Story admin → story page — FIXED ✅
- **Cause:** legacy `secret_stories_message_author_check` CHECK constraint allowed only authors `'T'`/`'Z'`; the facade admin logs in as `'J'`, so the `hidden_message` write threw and was silently swallowed.
- **Fix:** route now checks the error + falls back to writing without `message_author`; **migration 245** drops the constraint. Commit **f05b070f**.
- **OPEN:** apply **migration 245** in Supabase for the clean end-state (code fallback already works without it).

### 3. Voice Astra — REBUILT, then HIDDEN ⏸️
- 503 on start = missing **`AGORA_CUSTOMER_KEY` / `AGORA_CUSTOMER_SECRET`** in Railway → you added them (diagnostics commit **656a0f11**).
- Then "one moment please" every turn = Agora's anthropic binding missing the required `anthropic-version` header → fixed (commit **187a9850**).
- Then **rebuilt voice browser-native (dropped Agora entirely)**: Web Speech API → existing text brain → OpenAI TTS (`nova`), follows the cockpit language toggle (Chinese works). Commits **a3ab4eda**, **3a665f93**. Voice set to female `nova` (b78848e6).
- **Now HIDDEN** behind `SHOW_VOICE_ASTRA = false` in `app/montree/admin/page.tsx` (commit **2a3d4e7f**) until field-tested. Re-enable = flip the constant + redeploy.

### 4. "Can't find students by name" (e.g. Yo-Yo) — FIXED ✅ (verified)
- **Cause:** "Whale Class" classroom was **soft-deleted** (`is_active = false`), hiding the classroom AND its 22 still-active students from every school-scoped query. The student data + linkage were correct.
- **Fix (data):** reactivated only Whale Class (`51e7adb6-cd18-4e03-b707-eceb0a1d2e69`). Verified via the app API: active students **0 → 22**, Yo-Yo findable.
- **Systemic audit:** queried all schools for archived classrooms holding active students → **0 rows**. No other school affected.

### 5. Conversation persistence (uniform across all logins) — BUILT, migration pending ⏳
- Astra chat was browser-only (localStorage), so a thread/preloaded question on one device never showed on another login.
- Built: **migration 246** (`montree_principal_conversations`, per-principal), **`/api/montree/admin/astra-thread`** (GET active / PUT save / POST archive, fail-soft), and `page.tsx` wiring (load on mount with server-wins + never-clobber guard, debounced save, archive on New conversation). Commit **4059b833**.
- Code is **deployed + fail-soft** (behaves exactly as before until the table exists).
- **OPEN:** apply **migration 246** SQL in Supabase to activate persistence (provided separately). Then verify: ask in one browser, confirm it loads in another.

### 6. Decluttered the Today page — DONE ✅
- Hid the "Astra noticed / teachers haven't logged in" card behind `SHOW_ASTRA_PROACTIVE = false` (commit **2a3d4e7f**) to keep a newly-onboarded principal focused.

---

## OPEN action items
1. **Apply migration 246** (conversation persistence) in Supabase → then ping for end-to-end verify.
2. **Apply migration 245** (story author constraint) in Supabase — optional; code fallback works.
3. **Re-enable voice** (`SHOW_VOICE_ASTRA → true`) once browser-native voice is field-tested.
4. (Optional) Re-enable the idle-teachers card (`SHOW_ASTRA_PROACTIVE`) if wanted.
5. (Proposed, not built) Astra honest about archived classrooms; conversation-history UI + an Astra "recall past conversations" tool; `?ask=` deep-link that preloads a question.

---

## Key facts & gotchas (save these)
- **Prod Supabase project = `dmfncjjtsoxrnvcdnvjq` ("whale-class", org Tredoux555).** NOT `jeffy-commerce` (`inhrgiakjyprabxluppv`) — querying the wrong project returns misleading "0 rows". Always confirm the ref against `.env.local` (`NEXT_PUBLIC_SUPABASE_URL`).
- Migrations are applied **manually in the Supabase SQL editor** (DDL can't go over PostgREST).
- The local/agent shell has **no outbound internet** — run DB queries via the Supabase SQL editor, not curl.
- Build **ignores TypeScript errors** (`next.config.ts` `ignoreBuildErrors: true`); the repo carries large pre-existing lint/TS debt. Audit standard: **no NEW problems in changed files** (the supabase-untyped `never` errors are repo-wide and tolerated).
- Principal **direct-login link:** `https://montree.xyz/montree/login-select?code=<CODE>` auto-submits the code and lands them logged-in. (Principal Leu's code: `XVYHHX`.) Treat the link like a password — don't forward.
- Soft-deleting a classroom hides its whole roster; archived classrooms are currently invisible in the cockpit with no restore UI (candidate for future hardening).

## Commits this session (main)
`f05b070f` story+cockpit gate · `656a0f11` voice 503 names missing env · `4f338814` principal redirect-loop · `187a9850` voice anthropic-version header · `b78848e6` voice nova · `a3ab4eda` browser-native voice (drop Agora) · `3a665f93` voice follows language · `4059b833` server-side thread persistence (mig 246) · `2a3d4e7f` hide voice + idle-teachers card
