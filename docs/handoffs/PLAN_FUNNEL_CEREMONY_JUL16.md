# PLAN — FUNNEL CEREMONY (Lanternlight) — Jul 16, 2026
**Binding contract. Fable authored. Rebuilds the first-touch funnel to the APPROVED vision. Builders follow exactly — deviations need Fable sign-off.**

## §0 Approved vision (Tredoux: "this looks excellent - lock it in")
- **Mock (the visual law):** `docs/design/FUNNEL_VISION_JUL16.html` — open it, click through all 8 screens BEFORE writing code. Replicate its look, spacing, motion and copy placement.
- **Philosophy:** `docs/design/DESIGN_PHILOSOPHY_LANTERNLIGHT_CEREMONY.md`.
- **THE RULE (Tredoux, locked): the page speaks in HEADINGS ONLY; Astra speaks in sentences.** No explanatory subtext, helper lines, or note-cards on funnel pages. All explanation, reassurance and instruction lives in the top-left Astra narrator. Do not drift back.
- Astra is the ONLY guide, present top-left on EVERY funnel screen, with a working "Ask me anything".
- Related shipped system: onboarding copilot (`PLAN_ONBOARDING_COPILOT_JUL16.md`) — this contract also moves its dock to TOP-LEFT so the narrator hands over seamlessly at /admin.

## §1 Scope
Reskin + narrator on: `app/montree/try/page.tsx` · `app/montree/principal/setup/page.tsx` · `app/montree/login-select/page.tsx`.
New shared pieces: `components/montree/funnel/{funnel-theme.ts,GoldenThread.tsx,AstraNarrator.tsx}` + `app/api/montree/onboarding-copilot/ask-public/route.ts`.
**LOGIC IS UNTOUCHABLE.** Every API call, request/response shape, honeypot, banner rule, SSE parser, localStorage write, redirect and back-block behaves byte-identically. This is a skin + narrator, not a rewrite of behavior.

## §2 Shared framework (Build 1)
**`funnel-theme.ts`** — export `FT` tokens transcribed from the mock's `:root` + key styles (bg #030b08 gradient stack, vignette, gold #E8C96A/#cfa93f, action pill gradient `#27815a→#1D6B48`, glass `rgba(3,10,7,0.62)` edge `rgba(52,211,153,0.12)`, voice/whisper/hush text tiers, Lora/Inter stacks). Inline styles + ONE plain `<style dangerouslySetInnerHTML>` per page for keyframes (App Router: NEVER `<style jsx>` on these pages — landing-page CLS precedent).
**`GoldenThread.tsx`** — props `{step:1..6}`. Six nodes (i18n labels `copilot.funnel.thread.1..6`: You · Your school · Your key · Classrooms · Teachers · The handoff), done=gold dot, now=gold+glow, lit hairline between done nodes; ≤820px collapses to "Step N of 6 · Label". Pure presentational.
**`AstraNarrator.tsx`** — props `{screenKey, journey:'principal'|'teacher', authed:boolean, extra?:ReactNode}`. Renders the mock's narrator card: gold-A avatar, "Astra / Your front office" (i18n), narration from `copilot.funnel.say.<screenKey>`, Ask expander (typing shimmer → answer). Ask wiring: `authed=false` → POST `/api/montree/onboarding-copilot/ask-public` `{question, screen}`; `authed=true` → existing `/api/montree/onboarding-copilot/ask` `{question, journey, locale, screen}` (extend the authed route to accept optional `screen` and inject the funnel screen map into grounding when present — additive, no behavior change without it). Errors → `copilot.card.askError` line, never raw. Desktop: left column (sticky, 292px, grid `322px 1fr` like the mock). Mobile ≤1040px: full-width card ABOVE the stage content. Teacher journey variant: green "G" avatar + "Guru / Your mentor" (used on /try teacher path screens).
**`ask-public` route** — NO auth. Guards: `checkRateLimit(ip, 'copilot-ask-public', 6, 15min)` (reuse demo-request's limiter util; fail-open like it) + question cap 400 chars + honeypot field `website` (reject silently 200 with generic answer if filled). HAIKU_MODEL, max_tokens 300, temperature default. Grounding: STATIC English funnel map (the screens, what each asks for, the key facts: code=permanent login, email optional/recovery, computer-vs-phones, classrooms come stocked, teachers get 6-letter keys) + "never invent UI, answer in the user's language, ≤100 words, warm". `logApiUsage({schoolId:null…})` — check the helper's nullability; if schoolId is NOT nullable, log with a fixed sentinel: skip logApiUsage and instead `console.log('[copilot-ask-public]', tokens)` — do NOT crash. No DB reads. Response `{answer}`.
**Dock move (shipped copilot):** `CopilotDock.tsx` pill+card anchor bottom-left → **TOP-LEFT** (below `[data-dashboard-header]` on teacher dashboard — measure it, the Guru-viewport pattern; on admin below the topbar, left of/overlaying content start, NOT over the sidebar nav: `left: sidebar-width + 16px` on desktop admin — read the admin layout's sidebar width; mobile: below header, left 12px). Keep z-index 9000 and every other behavior identical.

## §3 Screen specs (match the mock 1:1)
**/try — role (`screenKey:'role'`)**: topbar (M artwork + wordmark, EN toggle preserved) · GoldenThread step 1 · hero M (use `/brand/m-mark.png` — transparent 512×420 already in public; Build 1 ALSO generates `/brand/m-mark-transparent.webp` (~70KB, width 640) from `~/Desktop/Montree Social Media Pack/montree-mark-transparent.png` via cwebp/PIL and prefers it w/ png fallback) with breathing halo (the ONLY halo) · H1 = `signup.welcome`-style heading ONLY ("Welcome." — new value on the existing heading key or new key `copilot.funnel.welcome`) · three role cards, principal GOLD-FEATURED w/ badge (i18n) — **PRESERVE: referral-code card-order swap, founding/referral banner logic (banners restyle to gold-tinted glass), Home/Parent card = teacher signup (unchanged)** · Astra says the S1 narration (two minutes, gold door, computer/phones).
**/try — details (`'details'`)**: heading "Name your school." ONLY (keep existing `signup.*` keys where labels; Email label/placeholder/helper — the helper line is REMOVED from the page, its content lives in Astra's S2 line; the hardcoded "Email" label may become i18n `copilot.funnel.email` or stay hardcoded as today — builder's choice, no regression) · slug preview ADDED under school name (pure client, `montree.xyz/<slug>` display only — display-only, do NOT wire it to anything) · honeypot untouched · CTA "Create my school →".
**/try — creating (`'creating'`)**: the ceremony — M + expanding rings + rotating Lora-italic lines (`copilot.funnel.cere.1..3`). NO spinner circles. Error path: keep the existing error+debug reveal + Try again (restyled glass, still shows `debug`/`steps` content).
**/try — code (`'code'`)**: H1 "Your school is founded." · gold keyplate (eyebrow YOUR PRINCIPAL KEY / for teacher role: YOUR TEACHER KEY via i18n) · big spaced mono code + Copy (existing clipboard logic) + "Enter your school →" (existing handleTakeMeIn, label per role) · NO keynote text on page — Astra says the save-it/administration line. Back-block behavior preserved.
**/principal/setup — classrooms (`'classrooms'`)**: GoldenThread step 4 · heading "Open your classrooms." ONLY · classroom rows per mock (emoji chip cycler, name input, jewel swatches w/ gold ring on selected — keep existing emoji/color state logic) · add-classroom dashed row · CTA.
**/principal/setup — teachers (`'teachers'`)**: thread step 5 · heading "Who teaches where?" ONLY · per-classroom glass groups (emoji+name header, teacher name+email rows, add-another) · NO gold note card (Astra carries the 6-letter-key line) · Complete setup ✓.
**/principal/setup — SSE overlay (`'founding'`)**: full-screen ceremony (M + rings + step line + REAL progress bar fed by the existing SSE `current/total` — keep the parser + cosmetic 2.5s ticker EXACTLY, restyle only).
**/principal/setup — success (`'handoff'`)**: thread step 6 · gold ✓ bloom (one 1.6s animation) · H1 "<School name> is founded." ONLY · **teacher key-cards** per mock (emoji · name · classroom · gold mono code · Copy · Send → existing per-teacher share/copy handlers; "Send" uses the existing share message) · wide gold group-chat CTA (existing copyAllCodes) · warnings block stays (restyled) · CTA **"Walk me in, Astra →"** → existing `/montree/admin` push · Astra narration = the keys/logins line. **DELETE the two dead `false &&` blocks (welcome overlay + PrincipalSetupGuide mount) and their orphaned state/localStorage writes** — sanctioned cleanup, this page only.
**/login-select**: same stage (dark ground, M small, glass card, mono code input restyled, gold focus) — logic untouched (QR autosubmit, unified auth branches, signup redirect). Astra narrator NOT required here (returning users) — a quiet single Astra hint line under the card is enough: `copilot.funnel.say.login` "Enter your key — the 6 letters you were given. Lost it? administration can get you back in." No ask box here.

## §4 Copy & i18n
- All new strings under `copilot.funnel.*` in ALL 12 locale files — EN + ZH real (您 for principal-facing funnel), 10 English-fallback. Narrations = EXACTLY the approved mock's NARRATE/ANSWERS text (transcribe, don't rewrite; ANSWERS become the static grounding examples, not UI strings).
- Existing `signup.*` / `principal.setup.*` keys: reuse where the mock kept the same meaning; when a VALUE changes (e.g. heading text), update EN+ZH real, set the 10 others to the new English. Never orphan a key.
- Astra celebration card copy on the old success screen is superseded — remove (it was hardcoded EN; no i18n debt).

## §5 Landmines
1. Honeypot `website` field: exact hidden styling + POST body, both /try and ask-public.
2. Founding/referral: banner precedence (founding wins), hidden on code step, card-order swap on referral — regression here breaks live outreach links.
3. SSE: manual reader/buffer parser + server-driven step overrides + `data.teachers`/`data.warnings` handling — byte-identical.
4. `handleTakeMeIn` localStorage writes + role-branch redirects — identical.
5. Back-block on creating/code steps — identical.
6. No `<style jsx>` on these pages; plain `<style dangerouslySetInnerHTML>` (SSR/CLS).
7. ask-public is anonymous: it must be added to middleware ONLY if middleware guards /api (it does not guard these API paths — verify, don't assume). Rate-limit + tiny max_tokens are the cost wall; never remove.
8. Mobile: teachers join on PHONES — /try must be flawless at 390px (narrator above stage, thread compact). Test by reading CSS, and note it for device verification.
9. Copilot dock move: do not change any logic, only anchoring; verify no overlap with admin sidebar or dashboard header.
10. New-user first paint: images — use width/height attrs on the M to avoid CLS.

## §6 Build order & verification
Build 1 (Opus): framework + ask-public + authed-ask `screen` param + dock move + /try + /login-select + ALL `copilot.funnel.*` keys ×12 + webp asset. Build 2 (Opus, after 1): /principal/setup on the shared framework (no new i18n files edits unless a missed key — then add ×12).
Gates: ESLint 0 errors on touched files · scoped tsc 0 new errors · i18n parity script green · Sonnet fresh-eyes FIX-FIRST audit vs this contract + the mock (visual conformance by reading styles, logic conformance by diffing behavior paths) · Fable final review of copy placement (headings-only rule) · Tredoux device walk (Mac + phone at 390px) before/after deploy.
