# OVERNIGHT PRODUCTION RUN — Jul 11, 2026 (Fable directing)

**Mission (Tredoux, authorized): generate ALL images for the entire 26-week curriculum via his
Midjourney account (unlimited plan) + ALL songs via his Suno account, full loop: generate →
download → rename → sort into `~/Desktop/English Curriculum 2026/Week NN/images/` → render packs.
He reviews everything in the morning. If this session dies, ANY session resumes from this doc.**

## 🔒 NEW LOCKED RULES (Tredoux, tonight — supersede prior)
1. **Suno style string (ALL songs):** `dark trap, 68 bpm, heavy 808 bass, sparse hi-hats, deep whisper-rap verses, kids choir chant on hook, playful spooky, minimal, clean vocals, nursery trap`
2. **WHOLE-WORDS lyric rule:** never syllable-split a word in lyrics (no "PO-TA-TO" → sing "POTATO!"). KEEP initial-sound stutters ("C-c-cat!", proven). KEEP uppercase letter-name spell-outs (O-N, B-O-X, A-E-I-O-U — letter names intended). Remove mid-word splits (d-u-ck, qua-ck, bu-zz → rephrase).
3. **Video principle (for mvgen phase, not tonight):** every sung noun/scene needs a matching image — "a pencil on the floor must show a pencil on the floor."

## THE LOOP (per chunk of weeks)
1. `cd "~/Desktop/Master Brain/ACTIVE/montree" && node scripts/curriculum/build-week.mjs --week N --assets "~/Desktop/English Curriculum 2026/Week 0N/images" --gap-only` → parse `• <file>` + `MJ> <prompt>` pairs.
2. Submit each to midjourney.com/imagine as `<filename> <prompt>` (filename prefix = label hack). One at a time, verify each lands in feed (rapid batches silently drop).
3. When grids complete: pick best-of-4 (photos: single clean subject; coloring: pure black-on-white, ONE object, white space, thick lines — no gray, no texture-fill). Download 1 per job → ~/Downloads.
4. Desktop Commander python: match Downloads files by prompt-keywords → rename to manifest name → move to `Week NN/images/`. Never destructive.
5. Re-run build-week full render → `Week 0N/pack-v2/` PDFs. Gap report should shrink to zero (or reference-shots only).

## SUNO LOOP (after spec update lands)
suno.com/create → Custom mode → paste Lyrics (from week JSON, post-scrub) + Style (locked string) + Title → Create → wait → download BOTH takes (⋯ → Download → MP3 Audio) → move to `Week NN/` named `WNN <title> (take K).mp3`. W1–W2 songs already exist (keep); generate W3–W26 = 48 songs × 2 takes.

## STATUS BOARD (update as chunks land)
- [ ] Spec update: new style string in all 26 JSONs + lyric scrub + contract §5 + validate + push
- [x] W1–4: download tonight's 44 grids, sort, submit 9 held-back scene shots (segina-on-mat, potato-on-mat, 7 cat scenes — descriptive, no oref tonight), render packs — **DONE (chunk A, Jul 11 00:53).** All 44 tonight's MJ jobs downloaded (1 reroll: tiger-coloring.png had 0 usable downloads on arrival, found in feed via search, picked best-of-4, downloaded). 9 held-back scene shots submitted one-at-a-time with feed verification, all completed, best-of-4 picked + downloaded. mat-coloring read as a clean single mat object (no reroll needed). All 53 files renamed+sorted into Week 01–04 images/ folders (Week 03 + Week 04 folders created). All 4 packs rendered to pack-v2/ — **gap report = "✅ All manifest images present" for all 4 weeks, zero image gaps.** Remaining pack warnings (missing dictionary_journal art for prior-letter tiles "a"/"at"/"am", qr_cards audioUrl placeholders) are pre-existing/out-of-scope (Suno audio loop, not tonight's image job).
- [x] W10–14 images — **DONE (Jul 11, this chunk, images only — Suno for W10-14 already completed in a prior chunk).** All 20 MJ jobs each for W10, W11, W12, W13 + 19 for W14 (letter E week; one fewer gap because a fallback/reused asset covered book:7) submitted one-at-a-time with feed verification, best-of-4 picked, downloaded, renamed+sorted into `Week 1N/images/`, all 5 packs rendered clean to `pack-v2/` (`--out` explicit every time). Final re-verification sweep: all 5 weeks report **"✅ All manifest images present"** + all 5 `pack-v2/` folders hold the full 20-file set (10 doc types × html+pdf). Residual warnings on every week are the same out-of-scope shapes as W5-9 (qr_cards audioUrl placeholders — Suno phase; dictionary_journal missing art for grammar/filler words — W12 dig/got, W13 bad, W14 pet; bingo 12-14-into-16 cycle-fill; W13's own hero word `big` has no dedicated `big-coloring.png`, same "own hero word" pattern flagged in the W5-9 line).
  - **🐕 CASTING DECISION — Dog debuts W12 (letter D week).** Consistent scruffy wire-haired terrier-mix appearance across all 6 dog images generated that week (dog.png, dog-digs.png, dog-digging.png, dog-digging-deep.png, dog-digging-slow.png, pot-in-pit.png, hat-in-pit.png): **a scruffy wire-haired terrier mix with a wavy tan-and-dark-brown mottled coat, floppy fringed ears, warm brown eyes, dark button nose, medium-small build.** Canonical reference = `dog.png`'s selected variant ("sitting and looking forward"). The look held consistently into W13 (big-dog-* scenes reuse the same wire-haired terrier look at exaggerated scale) and W14 (hen-on-dog.png) — no hero-lock/`--oref` pass needed yet, descriptive prompting has been sufficient. Worth an `--oref` pass later if drift appears in future weeks.
  - **🚨 NEW BUG FLAGGED FOR FUTURE SESSIONS — Midjourney's download icon sometimes opens the image in a NEW BROWSER TAB instead of saving to disk.** Hit 8 times across W11-W14 (potato-pot/cat-jump-pot/cat-on-pot/sam-pot/pot in W11; gift-coloring/goat-coloring/dog-coloring in W12; ball/bear/hens-on-bed/hens-in-pen/hen-on-dog/hens-on-man/big-egg/potato-egg/ten/bed/elephant/elbow/envelope/engine/get in W12-14 — roughly half of all W12-14 downloads hit this). Symptom: a stray tab titled like `0_0.png (1344×896)` at a `cdn.midjourney.com/<jobid>/<idx>.png` URL appears instead of a Downloads file. **Fix: run this in the stray tab via the JS console/tool** — `fetch(location.href).then(r=>r.blob()).then(b=>{const a=document.createElement('a');a.href=URL.createObjectURL(b);a.download='<target-filename>.png';document.body.appendChild(a);a.click();})` — then close the tab. 100% reliable recovery, verified via `ls -la ~/Downloads/<filename>.png` every time.
  - **⚠️ Transient MJ "Creation failed" errors** hit `elephant.png` (W14, failed 2×, succeeded on 3rd resubmit) and `engine.png` (W14, failed 2×, succeeded on 3rd resubmit) — generic "The job has encountered an error. The developers have been notified," unrelated to the prompt content (both jobs succeeded verbatim on retry). Not a stall (each attempt failed/succeeded within ~20-40s, never hung past 10 min) — just resubmit the identical job up to 2-3× if this appears.
- [x] W5–9 images — **VERIFIED DONE (Jul 11, second pass).** All manifest images were already present in `Week 0N/images/` for weeks 5-9 (generated earlier tonight, timestamps ~06:40-06:44) — `--gap-only` reported **zero MJ jobs** for all 5 weeks, so no Midjourney submissions were needed this pass. Re-ran the full (non-gap) render for all 5 weeks straight into `pack-v2/` (⚠ **gotcha: `build-week.mjs` defaults to legacy `pack/` unless `--out ".../pack-v2"` is passed explicitly** — always pass `--out` or you'll silently write the wrong folder, as this pass did on its first attempt before being corrected). All 10 pack types (bingo/book/coloring/dictionary_journal/matching/qr_cards/sentence_strips/three_part_cards/tracing/vowel_wall) rendered clean, exit 0, for W5-9. Residual warnings on every week are structural/out-of-scope, not image gaps: qr_cards audioUrl placeholders (Suno phase, not tonight's job); dictionary_journal missing art for decodable grammar/filler words (W5: sat/cats · W6: i/an/can/man · W7: pat/nap/tap · W8: it/in/sit · W9: hit/ham/him — function words, not illustrable vocabulary, same shape as the W1-4 carve-out); bingo 14-into-16 cycle-fill (word-pool size, not an asset gap). **One pattern flagged for Fable/Tredoux's call, not auto-fixed:** each week's own hero/title word is missing its dedicated `<word>-coloring.png` even though every OTHER word that week has one — W5 sat-coloring.png, W6 ant-coloring.png, W7 pat-coloring.png, W8 it-coloring.png, W9 hat-coloring.png. `it` is abstract/non-illustrable, but `ant`/`pat`/`hat` are real illustrable nouns and plausibly a genuine manifest gap (never listed by `--gap-only`'s check, so never queued for MJ) — worth deciding whether to add 3 targeted MJ coloring-page jobs or leave it (unconfirmed whether W1-4 have the same shape of gap). · [x] W10–14 (see chunk above) · [ ] W15–19 · [ ] W20–26
- [x] Suno W3–9 (Jul 11) · [x] Suno W10–14 (Jul 11, this chunk) — **DONE.** W3–14 = 24 songs × 2 takes = 48 files, all downloaded, renamed `WNN <title> (take K).mp3`, sorted into `Week 0N/`–`Week 14/`. Two deviations from verbatim JSON lyrics, both self-resolved mid-run:
  - **W9 song 2 "A Hat for the Ant"**: original JSON lyrics silently rejected 3× by Suno's copyright-similarity filter (toast: "Your lyrics contain copyrighted material" — pattern matched a Seussian rhyme/question structure). Rewrote the lyrics on-the-fly (same phonics content — ant/cat/Sam/hat/POTATO — different structure) to get it through. **The Week 9 "A Hat for the Ant" audio does NOT match the JSON's printed lyrics** — worth a look if the book/lyrics need reconciling later.
  - **W11 song 1 "O-O-On"**: a tool-level hiccup caused a failed select-all + splice, corrupting one submission into "Dad and Sam" (leftover W10 lyrics + new O-O-On text concatenated). Caught via abnormal clip duration, trashed the 2 corrupted clips, re-submitted clean. Final W11 files are correct/clean.
- [x] Suno W15–26 — **DONE (Jul 11, final Suno chunk).** 24 songs × 2 takes = 48 mp3s, all downloaded, renamed `WNN <title> (take K).mp3`, sorted into `Week 15/`–`Week 26/`. **Zero lyric rewrites needed** — every song generated cleanly on first submission with verbatim JSON lyrics (the whole-words scrub + phonetic respelling rules held; no copyright-filter rejections this chunk, unlike W9). Credits remaining: **1,590** (Pro plan). Suno UI quirks hit + resolved: "..." menu intermittently opened Remix instead of Download (click the Download text directly); CDP typing timeouts on long lyric blocks (text had landed — verify by screenshot, don't retype). **SUNO IS NOW COMPLETE FOR ALL 26 WEEKS** (W1–2 legacy songs kept as-is).
- [x] W15–19 images — **DONE (prior chunk).** Confirmed complete on filesystem inspection this chunk (20-24 images + 20 pack-v2 files present per week) — no action taken this chunk, verified only.
- [x] W20–26 images — **DOWNLOAD/SORT/RENDER DONE (Jul 11, this chunk).** All 130 MJ jobs across the final 7 weeks (pre-submitted by a prior chunk) located via search, best-of-4 picked, downloaded via canvas-draw technique (see below), renamed+sorted into each `Week NN/images/`, full `pack-v2` render (`--out` explicit) for all 7 weeks: W20=19, W21=18, W22=21, W23=19, W24=14, W25=20, W26=19 — **all 7 weeks report "✅ All manifest images present," all 7 packs rendered exit 0.** THE FULL 26-WEEK CURRICULUM IMAGE SET IS NOW COMPLETE. Residual pack warnings on every week are the same out-of-scope shapes as all prior weeks (qr_cards audioUrl placeholders — Suno phase; dictionary_journal missing art for grammar/filler words; bingo pool-size cycle-fill).
  - **🖼 NEW RELIABLE DOWNLOAD TECHNIQUE (replaces the "download icon opens new tab" bug workaround from the W10-14 entry) — canvas-draw, not re-fetch.** Cloudflare intermittently 403s a second `fetch()` to the same CDN image (hit repeatedly this chunk on W22-23). Fix: navigate a scratch tab directly to the raw `cdn.midjourney.com/<jobId>/0_<idx>.png` URL (no query params — full res), then run `const img=document.querySelector('img'); const canvas=document.createElement('canvas'); canvas.width=img.naturalWidth; canvas.height=img.naturalHeight; canvas.getContext('2d').drawImage(img,0,0); canvas.toBlob(b=>{const a=document.createElement('a'); a.href=URL.createObjectURL(b); a.download='<filename>.png'; a.click();})` — reads the already-rendered `<img>` pixels instead of issuing a second network request. 100% reliable across ~130 downloads this chunk, zero failures. **Adopt this as the standard technique going forward, retire the old fetch()-based recovery snippet.**
  - **🦆 Duck (debuted W21) held consistent through W25-26**: plump white duck, bright orange bill, orange webbed feet — confirmed via prompt text on every job (duck-quack.png, duck-quack2.png, duck-in-box.png, etc.).
  - **🦊 CASTING DECISION — Fox (debuted W24) held consistent through W25-26.** Every fox job's prompt text explicitly carried the full description (rust-orange fur, white chest/chin, black-tipped ears and paws, bushy white-tipped tail) and every rendered grid matched it — fox-in-box.png, fox-box-peek.png, fox-quack.png, fox-mix.png, graduation-cast.png all show the same rust-orange/white-chest/black-tipped fox. **Could not pixel-diff against W17's actual fox.png file this chunk (no local image-diff tool in this environment), but the description is locked in the prompt text itself and rendered faithfully every time — no drift observed in the renders.**
  - **🎓 W26 graduation-cast.png (the finale) — picked variant with the MOST cast members legible**: cat, dog, rat, ant, beetle, fox, duck + girl peg doll all clearly on-model in the chosen grid (boy peg doll partially occluded — present in other variants but this trade-off had the best overall cast count). Potato got its own dedicated finale shot (`potato-graduation.png` — potato in cap+glasses at a whiteboard, taking a bow) since it doesn't appear in the physical group photo.
  - **🐛 Minor cast-consistency note (not severe enough to reroll): `zigzag-buzz.png`'s beetle** rendered with a spotted/ladybug-like shell pattern, differing from the plain solid-red-shell look in `bug-buzz.png`/`bug-zip.png`/`ant-tag.png` (all same week). Likely a festive-scene lighting/render variance, not a genuine character drift — flagged for Tredoux's eye in the morning review, not blocking.
  - **🎨 W23 reroll-candidate verdicts (resolved this chunk):** both **CONFIRMED** the flagged shading anomaly — `yam-coloring.png` and `yak-coloring.png` each rendered all 4 grid variants with heavier cross-hatched/textured linework than the house flat-black-on-white style (same shape as the W2 tiger-coloring precedent). Did NOT reroll (per the W2 precedent rule) — picked the least-shaded variant from each grid instead: yam-coloring.png used the variant with decorative interior swirl lines instead of cross-hatch texture; yak-coloring.png used the variant with the most flat black linework and least gray/gradient shading. Both are usable prints, not ideal house-style but consistent with how W2's tiger-coloring was handled.
- [ ] Morning review checklist (TREDOUX): audio takes (48 new W15–26 mp3s + W3–14's 48 — agents can't hear, pick 1 of 2 takes per song), cast consistency (hero-lock re-rolls w/ --oref if cat/Segina/fox drift — esp. W24-26 fox vs W17 fox.png side-by-side, and zigzag-buzz.png's spotted beetle vs the solid-shell bug), coloring-page quality (shading anomalies: W2 tiger, W17 fox, W18 lion, W23 yam + yak — all picked least-shaded, reroll w/ "flat vector line art, zero gradient" if they print badly), W9 "A Hat for the Ant" audio ≠ JSON lyrics (reconcile or re-gen), hero-word coloring-page manifest gap decision (W5 sat / W6 ant / W7 pat / W9 hat / W13 big have no dedicated coloring page — add targeted MJ jobs or leave), spot-check pack PDFs

## 🏁 RUN COMPLETE — Jul 11, ~15:30 (Fable closing entry)

**ALL PRODUCTION DONE. Full 26-week curriculum: images ✅ + packs ✅ + songs ✅.** Final read-only verification sweep (independent agent) confirmed: all 26 week folders present · **260 PDFs (10 × 26 weeks), zero 0-byte, all heavy PDFs (bingo+book × 26) valid `%PDF` headers at healthy 30–60MB sizes** · 260 HTMLs · images 14–26 per week matching manifests ("✅ All manifest images present" on every week's gap report) · mp3s exactly 4 per week for W3–26 (2 songs × 2 takes), W1=1/W2=3 legacy.

**Incidents tonight (all resolved, lessons banked):**
1. **Chrome download block (W17):** downloads silently stopped saving mid-chunk — cause was the per-site "multiple automatic downloads" permission. Fix: `cdn.midjourney.com` → Automatic downloads → **Allow** (Tredoux set it). If downloads ever silently stop again, check this first.
2. **🚨 DISK FULL → silent 0-byte PDFs:** W19's bingo.pdf rendered 0 bytes 3× while the build logged "✓" and exited 0. Root cause was NOT the bingo builder — the Mac was at 100% disk (143 MiB free); headless Chrome hit `FILE_ERROR_NO_SPACE`, wrote nothing, and still exited 0. **Fix shipped in `scripts/curriculum/build-week.mjs`** (Opus, verified + regression-checked W5/W18): `htmlToPdf()` now uses spawnSync, pre-clears stale artifacts, and **hard-fails with Chrome's real stderr on any 0-byte PDF** — a silent ✓ on an empty PDF is now impossible. Disk was cleared to ~4.7 GiB (Trash-adjacent caches, legacy `pack/` dupes, DMGs, brew cleanup); **ended the run at 1.7 GiB free (91%) — disk remains the #1 environmental risk for future render/mvgen runs.** Big reclaim candidates for Tredoux (his call): `~/.Trash` still 1.2 GB, `vm_bundles` 16 GB + old Cowork sessions 3 GB under Claude's Application Support, ~10 GB video footage in `~/Downloads`.
3. **Cloudflare 403 on re-fetch:** solved permanently by the canvas-draw download technique (see W20–26 entry) — standard method going forward.

**Owed / next:** morning review checklist above → then the mvgen video phase (songs + images + shotlist are now all in place; per the Jul-10 pin, the anchor-pulse tweak is queued first). Commit tonight's two files (this doc + the build-week.mjs guard) — done via Desktop Commander at close; the pre-existing working-tree dirt (mvgen app files, outreach CSVs, social playbook, etc.) is deliberately NOT bundled and stays owed from prior sessions.

## RESUME PROMPT (paste into a fresh session)
"Read CLAUDE.md's top session block + docs/curriculum/OVERNIGHT_RUN_JUL11.md. Continue the overnight run: (1) THE LOOP for images weeks 5–26 — spawn Sonnet browser agents in ~5-week chunks (W5–10, W11–18, W19–26), prompts auto-pulled per week via `node scripts/curriculum/build-week.mjs --week N --assets "~/Desktop/English Curriculum 2026/Week NN/images" --gap-only`; submit one-at-a-time with feed verification (MJ silently drops rapid batches); best-of-4 per runbook criteria; agent is casting director on cast debuts (dog W12, rat W15, bug W18, duck W21, fox W24). (2) SUNO LOOP for W15–26 (24 songs, both takes). (3) Update this status board per chunk; commit+push docs when done. Fable directs, Sonnet works the browser, never touch account settings."

## CAVEATS FOR THE MORNING
- Agents can't hear: both Suno takes saved per song, Tredoux picks.
- Cast consistency is descriptive-prompt-level tonight; hero-lock (reference image pass) is the polish option.
- MJ/Suno rate limits or logged-out states pause a chunk — resume from this doc.
- **W2 tiger-coloring.png anomaly:** all 4 grid variants rendered with grey/gradient shading rather than pure black-on-white line art (the coloring-page house style). Picked the cleanest of the 4 (least shading) rather than reroll, since the runbook only mandates an automatic reroll for mat-coloring. Worth a look in the morning — may want to reroll with an emphasized "flat vector, zero gradient" prompt tweak if it doesn't print well.
