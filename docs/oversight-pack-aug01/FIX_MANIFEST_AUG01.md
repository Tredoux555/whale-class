# Backend fix manifest — Montree (P0 data loss + security hardening)

One line per changed/created file. Scoped typecheck (`tsconfig.fixcheck.tmp.json`,
now deleted) reported **0 new errors in any edited file**. Output was confined to
pre-existing errors in untouched `lib/montree/curriculum-loader.ts`,
`curriculum-data.ts`, `voice/observation-analyzer.ts`, plus one pre-existing
styled-jsx typing error at `app/montree/super-admin/page.tsx(1015)` — the
`<style jsx global>` block at the bottom of the file, ~750 lines below any edit
made here and unrelated to it.

## Created

- `migrations/311_progress_stamp_columns.sql` — **NEW.** Idempotent `ADD COLUMN IF NOT EXISTS` for `classroom_id UUID`, `work_key TEXT`, `school_id UUID` on `montree_child_progress`, plus `idx_montree_child_progress_classroom` on `(classroom_id)`. Header comment documents the P0. **Risk:** low — purely additive, all columns nullable, no backfill, `(child_id, work_name)` upsert key untouched, safe on a live table. Must be run BEFORE the fix has any effect: until it runs, every progress upsert from paper-scan and voice commits fails 42703 while the scan/session is still marked committed and the photo/audio is already deleted (unrecoverable).
- `migrations/312_minimal_signup_defaults.sql` — **NEW.** `UPDATE montree_feature_definitions SET default_enabled = false` for `games, class_events, bulk_student_import, multi_child_tagging, print_weekly_plan, group_lesson_suggester, home_practice_cards, multi_teacher_mgmt`. Header lists what deliberately stays ON (`photo_audit, photo_pipeline_v2, daily_reports, teacher_notes, parent_portal, onboarding_copilot`) and invites the founder to edit the `IN (...)` list before running. Follows migration 280's pattern (BEGIN/COMMIT, idempotent). **Risk:** medium-behavioural, zero-technical — schools relying on the *default* (no `montree_school_features` override) will lose those menu items; schools with an explicit override are unaffected. All 8 keys verified to exist in `lib/montree/features/types.ts` and/or prior migrations.

## Modified

- `app/api/montree/paper-scan/[scanId]/commit/route.ts` — FIX 1 hardening only. Added `progressFailed` counter, incremented in the existing `progressError` branch (which already logged + pushed to `errors`), and added `progress_failed` to the JSON response. **Risk:** very low — purely additive field; `success: true` and all existing response keys unchanged so the (concurrently edited) paper-scan page keeps working. Note: the `advanceProgressOnConfirm()` path returns `void` and still has no error surface — left alone deliberately (out of scope / would require touching `lib/montree/progress/advance-on-confirm.ts`).
- `app/api/montree/voice-observation/[sessionId]/commit/route.ts` — FIX 2 + FIX 1 hardening. Added module-level `STATUS_RANK` (copied verbatim from paper-scan) and wrapped the progress upsert in the same never-downgrade gate: fetch existing `status` by `(child_id, work_name)` via `.maybeSingle()`, write only when `isTeacherDecision || !existingProgress || nextRank > currentRank`. Added `progressFailed` counter (incremented in the existing `progressError` branch) and `progressFailed` in the response. **Risk:** low-medium — behaviour change is intentional (a recording proposing `presented` no longer overwrites `mastered`); `teacher_final_status` still always wins, matching paper-scan. Skipped-because-already-higher extractions still count toward `committedCount`, so the existing `committedCount === 0` → 500 guard does not newly fire. One extra SELECT per extraction (same cost paper-scan already pays).
- `middleware.ts` — FIX 3 (+ review round 2). Hoisted the media test into `const isMediaApi = pathname === '/api/media' || pathname.startsWith('/api/media/')`, added it to the `requiresAdminJWT` predicate, and added `'/api/media'` + `'/api/media/:path*'` to `config.matcher`. **Review fix:** the admin-token cookie alone was too narrow — the `/admin` PAGE gate further down also admits a verified Supabase session whose `user_roles` include `admin`/`super_admin`/`teacher`, and those users would have 401'd on every `/api/media` call mid-page. Added a module-level `hasSupabaseAdminRole(req)` helper that mirrors the page gate exactly (same token sources — `authorization` Bearer, `sb-access-token`, `supabase-auth-token`; same `setSession` + `getSession` + `user_roles` query; same 3s `withTimeout` on each; same three-role list) and the gate now reads `if (!isMediaApi || !(await hasSupabaseAdminRole(req)))` before 401ing. **Only `/api/media` gets this second door** — `/api/admin/*`, `/api/whale/*` and the legacy groups keep their strict admin-token-only check, so no existing gate was loosened. Helper returns `false` (deny) when `supabaseUrl`/`supabaseAnonKey` are unset, unlike the page gate which allows through at build time — a deliberate fail-closed divergence, since an unauthenticated media mutation surface is exactly what this fix closes. **Risk:** low-medium; the added path costs up to three awaited Supabase calls, but only for `/api/media` requests that lack a valid admin-token, and each is timeout-bounded at 3s. Caller audit: `app/admin/child-media/page.tsx`, `app/admin/classroom/[childId]/page.tsx` (GET/POST/DELETE/PATCH), `app/admin/classroom/student/[id]/page.tsx`, `app/admin/hub/page.tsx` — **all under `/app/admin/*`, which already requires the `admin-token` cookie**. `app/admin/mvgen/page.tsx` calls `${DAEMON}/api/media` — a different origin (the local `scripts/mvgen/server.py` daemon), unaffected. `components/classroom/PhotoCapture.tsx` also posts to `/api/media` but **has no importers anywhere in `app/` or `components/`** — dead component. **No teacher/parent/montree-surface caller found**, so the sub-fix was applied rather than stopped.
- `app/api/montree/community/works/[id]/inject/route.ts` — FIX 4. Added `checkRateLimit(supabase, ip, '/api/montree/community/works/inject', 5, 15)` at the top of POST with a 429 + `Retry-After` response, mirroring `app/api/montree/community/works/route.ts` exactly (same imports: `@/lib/rate-limiter`, `getClientIP` from `@/lib/montree/audit-logger`). **Risk:** low — fail-OPEN default preserved, so a limiter-table outage never blocks a genuine inject. 5/15min per IP is deliberately tight because the only credential is a 6-char `login_code`.
- `app/api/montree/super-admin/demo-requests/bulk-reply/route.ts` — FIX 5. Added `isProviderCapacityError()` helper (matches `rate_limit`, `rate limit`, `too many requests`, `quota`, `daily limit`, `429`, case-insensitive) plus `abortedReason` / `notAttempted`. On a matching Resend error the loop stops sending; remaining leads are pushed as `skipped: true` outcomes with reason `not attempted — batch stopped (...)`. Response gains `not_attempted` and `aborted_reason` (now consumed by the UI — see next entry). **Risk:** low — `sent`/`failed`/`skipped`/`outcomes` keys unchanged, so the existing super-admin client still reads. Note the aborting lead itself is still counted in `failed` (it genuinely failed); the untouched remainder is counted separately in `not_attempted`. String matching on the provider message is heuristic — a novel Resend wording could slip through and the batch would continue as before (i.e. degrades to old behaviour, never worse).
- `app/montree/super-admin/page.tsx` — FIX 5 UI surfacing (review round 2). The circuit breaker's `not_attempted` / `aborted_reason` previously died in the JSON — the operator saw only "N sent, M failed" and had no way to know the batch had stopped early. Extended the existing `lastBulk` state shape with `notAttempted: number` / `abortedReason: string | null`, populated both from the response (and zeroed them in the `!res.ok` branch), and added one red banner below the existing emerald summary line, rendered only when `abortedReason` is set: shows the provider's message, how many leads were not attempted, and tells the operator to re-run once the limit resets. **Risk:** very low — additive state fields and one conditional block inside the existing `DemoRequestAlert` component; the existing summary banner is unchanged. This file is NOT one of the five owned by the concurrent frontend agent. Note `app/api/.../bulk-reply` returns snake_case (`not_attempted`) while the component state is camelCase (`notAttempted`) — the mapping happens at the single `setLastBulk` call site.
- `app/api/weekly-planning/upload/route.ts` — FIX 6a. Added `MAX_UPLOAD_MB = 15` / `MAX_UPLOAD_BYTES` constants and a `file.size > MAX_UPLOAD_BYTES` → `413 { error: 'File too large — the limit is 15MB.' }` check, placed after the `!file` guard and **before** `arrayBuffer()` so nothing oversized is buffered or sent to Claude. **Risk:** very low — matches the route's existing `{ error: string }` response shape.
- `app/api/curriculum-import/works/route.ts` — FIX 6b. Added `MAX_FILES_PER_UPLOAD = 40` and a `files.length > 40` → `400 { error: 'Too many files — upload at most 40 at a time.' }` check right after the empty-files guard. **Risk:** very low — each file costs 2 AI calls + a storage upload inside one request, so the cap protects the request timeout and the Anthropic bill; matches the route's existing `{ error: string }` shape.

## FOUNDER MUST RUN (paste in Supabase SQL editor, in this order)

1. **`migrations/311_progress_stamp_columns.sql` — URGENT / P0.** Until this runs, every paper-scan and voice-observation commit silently drops the teacher's approved progress (Postgres 42703) *after* the source photo/audio has already been deleted. Idempotent, additive, safe on a live table. Run this first, before deploying or immediately after.
2. **`migrations/312_minimal_signup_defaults.sql` — read the header first.** Flips 8 feature defaults to OFF for new schools. The `IN (...)` list is a judgement call — edit it before running if you want any of them to stay on. Per-school overrides in `montree_school_features` are unaffected.
3. **`migrations/308_paper_scan.sql` — VERIFY IT HAS BEEN RUN.** It is the newest pre-existing numbered migration and `docs/handoffs/PLAN_CELLPHONELESS_CLASSROOMS_JUL30.md` lists "migration 308 pasted in chat" as a build step, with no record here of it actually being applied. It creates `montree_paper_scans` / `montree_paper_scan_extractions` and seeds the `paper_scan` feature flag — the paper-scan commit route cannot work without it, and 311 is meaningless if 308 never landed. It is idempotent, so re-pasting it is safe if you are unsure.

## Not done / notes

- `advanceProgressOnConfirm()` (`lib/montree/progress/advance-on-confirm.ts`) returns `Promise<void>` and swallows its own errors, so the no-status branch of the paper-scan commit still cannot detect a failed write. Fixing it means changing that helper's signature and every caller — deliberately left out of a surgical pass. Worth a follow-up.
- Neither commit route was changed to *refuse* to mark the scan/session `committed` on partial failure. That would strand teachers in a half-committed state with the source media already deleted and needs a retry design, not a one-line patch. The minimal hardening (log + count + report) was applied per instruction, so a failure is now visible in the response instead of silent.
- Files owned by the concurrent frontend agent were not touched: `components/montree/DashboardHeader.tsx`, `lib/montree/features/menu-sync.ts`, `app/montree/admin/layout.tsx`, `app/montree/admin/features/page.tsx`, `app/montree/dashboard/paper-scan/page.tsx`.
# Frontend / feature-system fix manifest

One line per changed file: path — what changed — risk notes.

## Changed files

- `components/montree/DashboardHeader.tsx` — **FIX A + FIX C.** (A) Added `menuReloadNonce` state + `lastMenuFetchRef` and a second effect that listens on `window 'focus'` and `document 'visibilitychange'`, bumping the nonce (which is now a dep of the existing `/api/montree/teacher/menu` effect). Throttled by module constant `MENU_REVALIDATE_MS = 10_000` and skipped when `document.visibilityState === 'hidden'`, mirroring `lib/montree/features/context.tsx`. The original `cancelled` guard and error handling are untouched. (C) Deleted the twelve dead `isEnabled('menu_*')` menu rows (`menu_notes`, `menu_curriculum`, `menu_guru`, `menu_photo_audit`, `menu_manage_students`, `menu_focus_list`, `menu_photo_albums`, `menu_library`, `menu_class_progress`, `menu_language_semester`, `menu_earnings`, `menu_classroom_setup`) and replaced them with a 🪦 comment explaining why they could never render. Removed the six lucide imports they were the last consumers of (`Target`, `Sparkles`, `FolderOpen`, `TrendingUp`, `Users`, `BarChart2`) plus `Images` (its only other reference is inside a JSX comment), with a note on how to re-add. **Risk:** low. Verified the delete is safe two ways — (1) `GET /api/montree/features` builds its payload by `.map()`ing over `montree_feature_definitions` rows, and no migration ever inserts a `menu_*` key, so `isEnabled('menu_*')` is structurally always `false`; (2) the surrounding `menuConfig === null` branch IS still reachable (teacher-menu fetch failure / offline), so I did **not** delete the branch itself — the ungated rows (Classroom Overview, Calendar, Messages, Parent Manager) and the rows on *registered* keys (`weekly_admin_docs`, `raz_reading_tracker`, `english_corner`, `paperwork_tracker`) are all kept. Focus revalidation adds at most one GET per 10s per focused tab.

- `lib/montree/features/menu-sync.ts` — **FIX B.** Added a bold header comment stating this map is the ONLY bridge from a feature flag to a teacher menu row, and that adding `isEnabled(...) && <MenuRow>` to DashboardHeader does nothing for teachers with a saved config. Added six new pairs, all keys verified present in `montree_feature_definitions` via the migrations: `guru_advisor→guru`, `teacher_notes→notes`, `photo_audit→photo_audit`, `curriculum_browser→curriculum`, `community_library→library`, `classroom_setup_ai→classroom_setup` (all migration 149). Legacy `menu_*` pairs retained (inert) and labelled as such. Expanded the "deliberately absent" section. **Risk:** low-moderate. All six keys were previously referenced *nowhere* in application code (only in `types.ts`) — they were switchboard toggles that did literally nothing, which is the founder's complaint. Sync only runs on an explicit toggle, so nothing changes for existing schools until someone flips a switch. Consequence to know: a super-admin "disable all" will now hide Guru / Notes / Curriculum / Photo Audit / Library / Classroom Setup from every teacher's saved menu (re-enabling restores them — `syncTeacherMenusForSchool` sets `visible:false`, never removes).

- `app/montree/admin/layout.tsx` — **FIX D (part 1).** Added `Sliders` to the lucide import, a `selfServe` state, a `useEffect` (gated on `authState === 'authed'`) that probes `GET /api/montree/school-features` and sets `selfServe = res.ok`, and a conditional `activeNav` entry "School Features" → `/montree/dashboard/school-features` using the existing `schoolFeatures.menuLabel` i18n key. **Risk:** low; fails closed (any error → row hidden). **Deviation to note:** the brief suggested reusing `hooks/useFeatures` / `GET /api/montree/features`. I could not — `feature_self_serve` has NO row in `montree_feature_definitions` (it is a bare `montree_school_features` override; see the header of `app/api/montree/super-admin/school-features/route.ts`), and `/api/montree/features` maps over the definitions table, so that gate would be permanently false. `/api/montree/school-features` is the pre-existing gate (403 `self_serve_disabled` / 200) and derives the school from the auth cookie, so no `school_id` plumbing. Also: `/montree/admin/*` has no `FeaturesProvider`, so `useFeatures()` is unavailable there anyway. Rationale is in a comment at the effect.

- `app/montree/admin/features/page.tsx` — **FIX D (part 2).** Added a file-header note marking it a hidden legacy surface (classroom-level flags, no menu sync). Added a `selfServe: 'checking' | 'on' | 'off'` state fed by the same `GET /api/montree/school-features` probe, folded `'checking'` into the existing loading screen, and added a locked state (reusing existing `schoolFeatures.lockedTitle` / `lockedBody` keys) rendered when Give Control is off. **Risk:** low; client-side visibility gate only — authoritative enforcement for school-wide toggles stays in the API. Note this page's own POSTs still go to `/api/montree/features` (classroom scope), which does not enforce `feature_self_serve`; I did not change that route (backend agent's territory and out of brief).

- `app/montree/admin/settings/page.tsx` — **FIX D (part 2), hide-don't-delete.** Commented out the `ManageLink href="/montree/admin/features"` ("Feature flags") row with a note on why and how to restore. This was the *only* nav entry to the legacy page (the admin layout merely `match`es its path under Settings). **Risk:** none; `Sliders` import is still used by the section header on the same page. **Note:** this file was not on the assigned list, but it is a frontend page (no overlap with the backend agent's api-route/migration scope) and it is where the nav entry actually lives.

- `app/montree/dashboard/paper-scan/page.tsx` — **FIX E.** Imported `isNativeCameraAvailable` / `captureNativePhoto` from `lib/montree/platform/camera`. Added `handleCameraClick`: if native, `captureNativePhoto({ facing: 'environment' })` → wrap the returned blob in a `File('record-sheet.jpg')` → feed the SAME `handleFile()` compress+upload path used by `onInputChange`; a Capacitor cancel (`err.message.includes('cancelled')`, the house pattern from `CameraCapture.tsx`) returns silently, any other failure falls back to `cameraInputRef.current?.click()`. Non-native still clicks the input exactly as before. Wired the camera button to it. Added a `showDesktopHint` state computed in an effect (`!isNativeCameraAvailable()` && no coarse pointer && `navigator.maxTouchPoints === 0`) rendering `t('paperScan.desktopHint')` under the camera button. **Risk:** low; the gallery button, the hidden inputs and the upload path are untouched, and the web path is byte-identical in behaviour. The hint is computed in an effect so SSR and first client paint agree.

- `app/montree/dashboard/paper-scan/page.tsx` (2nd pass) — **FIX F — surface `progress_failed`.** Added `progress_failed: number` to the `CommitResult` interface, read `data.progress_failed` in `commit()`, and branched the post-commit toast: `progress_failed > 0` → `toast.warning(t('paperScan.progressFailed'), { duration: 10000 })` instead of the green `commitSuccess` toast. Also added a persistent amber notice on the DONE screen (same amber treatment the review screen uses for low-confidence reads) so the warning survives the toast expiring. **Risk:** low; purely additive, and `Number(...) || 0` means an older backend that omits the field behaves exactly as before (0 → success toast, no notice). **Why both toast and notice:** the commit route marks the sheet `committed` regardless, so a toast the teacher misses is the teacher's last chance to learn records were lost.

- `components/montree/voice-observation/VoiceObservationReview.tsx` — **FIX F — surface `progressFailed`.** In `handleCommit`, on `data.success`, read `data.progressFailed` (camelCase — that is the shape `app/api/montree/voice-observation/[sessionId]/commit/route.ts` returns, unlike paper-scan's snake_case) and fire `toast.warning(t('voiceObs.progressFailed'), { duration: 10000 })` **before** `onCommitted()`, because the parent page fires its own `voiceObs.commitSuccess` toast and sonner stacks — the warning has to already be up. Kept the component's existing `t('key') || 'English fallback'` convention. Added `t` to the `useCallback` deps. **Risk:** low; additive, and `Number(... ?? 0) || 0` is backward-compatible. This one matters more than paper-scan's: the voice route deletes the audio and transcript on commit, so an unsurfaced failure is unrecoverable.

- `lib/montree/i18n/{en,zh,es,de,fr,pt,nl,it,ja,ko,uk,ru}.ts` — **FIX E + FIX F support.** Added three keys across all 12 locales, each translated (not English-stubbed) and inserted after an existing anchor key: `paperScan.desktopHint` (after `paperScan.chooseFromGallery`), `paperScan.progressFailed` (after `paperScan.commitSuccess`), `voiceObs.progressFailed` (after `voiceObs.commitSuccess`). Both `progressFailed` strings use the page's `{count}` placeholder + `.replace()` convention. `npm run i18n:check` passes at 100% parity for all 12 (5528 keys each). **Risk:** none.

## Deliberately NOT done

- **Did not delete the `menuConfig === null` legacy branch itself.** It is genuinely reachable — if `/api/montree/teacher/menu` fails (offline, cold start, 500) `menuConfig` stays `null` and this branch is the teacher's entire menu. Only the twelve provably-unreachable rows were removed; per the brief's "gate-and-keep if reachable" instruction the rest stays.
- **Did not remove `isEnabled('language_presentation')`.** It is orphaned the same way (no definition row) but was not among the twelve keys enumerated in the brief, so I left it and flagged it here instead. Same for `menu_classroom_overview` (already absent from the header).
- **Did not fix the teacher-side `isEnabled('feature_self_serve')` School Features row in DashboardHeader.** It is dead for exactly the same reason (no definition row → never returned by `/api/montree/features`). Fixing it the way the admin layout does would add a network call to the most-mounted component in the app. Documented in place with a ⚠️ comment; the cheap real fix is a migration inserting a `feature_self_serve` definition row (backend agent's territory).
- **Did not map `parent_messaging`, `school_calendar` or `parent_portal` to menu items.** All three are real registered keys, but each already gates a PARENT-facing surface server-side (`lib/montree/parent-messaging/access.ts`, `app/api/montree/parent/calendar/route.ts`). Mapping them would let a parent-visibility toggle silently delete a teacher's menu row. Rationale recorded in the map's comment.
- **Did not invent keys for `manage_students`, `focus_list`, `photo_albums`, `class_progress`, `language_semester`, `earnings`.** No registered feature definition owns them; per the brief, no invented keys.
- **Did not touch `app/api/montree/teacher/menu/route.ts` or `app/api/montree/features/route.ts`.** No change was needed for these fixes.
- **Did not fix `lib/montree/platform/camera.ts:67`** (`presentationStyle: 'fullScreen'` should be `'fullscreen'` — TS2820). Pre-existing error in an untouched file that is already in the repo's compile graph and already used in production by `CameraCapture.tsx`; out of scope, flagged here.

- **Did not add a `progress_failed` notice to the voice-observation DONE screen** (`app/montree/dashboard/voice-observation/page.tsx`), only the toast. The count is not threaded through the component's `onCommitted(count: number)` callback, and widening that prop contract plus adding state to the parent page is more surface than "minimal surfacing" warrants. The 10s warning toast fires on the same screen transition. If a persistent notice is wanted there, the change is `onCommitted(count, progressFailed)` + one amber block in the `pageState === 'committed'` branch — flagged, not done.
- **Did not touch either commit API route.** Both already return the counts; the gap was purely that no UI read them.

## Verification

Scoped typecheck via `/tmp/montree/tsconfig.fixcheck2.tmp.json` (extends `./tsconfig.json`, `"include": []`, `"files": [`all edited files + `menu/registry.tsx`, `menu/config.ts`, `school-features/page.tsx`, `voice-observation/page.tsx`, all 12 locales`]`, `types: ["react","node","styled-jsx"]` so styled-jsx JSX props resolve without `.next/types`). Result: **1 error, `lib/montree/platform/camera.ts(67,5) TS2820` — pre-existing, untouched file.** Zero errors in any edited file. Temp tsconfig (and its `.tsbuildinfo`) deleted. `npm run i18n:check`: all 12 locales 100% (5528 keys). `toast.warning` confirmed available (sonner 2.0.7, already used at `app/montree/dashboard/[childId]/weekly-review/page.tsx:223`).
