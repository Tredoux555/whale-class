# Montree — Handoff (2026-06-06)

*Single current-state doc. Supersedes START_HERE.md. Everything below is LIVE on production unless marked otherwise.*

## ✅ Phonics-as-Works — LIVE, seeded for Whale Class, audited 100% clean

The thing you actually wanted: **snap a photo of a child doing a phonics lesson → it identifies the work → tracks per child.** Now fully wired and verified.

**Audit found (and we fixed) a real gap:** the first version made the 49 works *virtual* (no DB rows). They showed in the picker, but the AI "snap" flow, the auto-progress gate, the shelf, and the work-name labels all key off the real curriculum table — which virtual works aren't in. So snap-and-track never actually fired for phonics. **Fix: seeded them as real curriculum rows** (`source='phonics_pack'`), so they ride every existing rail with zero special-casing.

- The 49 Pink lessons (5–53) are real Language-area works ("Phonics 40: minimal pairs — cat, cot, cut", etc.), seeded into Whale Class.
- Snap a photo → the AI now sees phonics in its candidate list → auto-advances that child's progress (`montree_child_progress`: presented → practicing → mastered) like any work. Manual tagging works too. Same dashboards/reports.
- **Toggle**: flipping `phonics_works` in super-admin now **self-seeds** the rows (on) or **hides** them (off, non-destructive — progress survives). One toggle, no SQL.
- **Verified**: `scripts/verify-phonics.ts` → 49 in picker, no dupes, all resolve to Language, AI sees them. ✅ 100% clean.

**Known follow-up (not done):** *inline song player* on the work-detail screen (UI spread across components — do it with the app running). For now the song/book link rides in each work's description (montree.xyz/dark-phonics.html).

**Files:** `lib/montree/phonics/phonics-works.ts` (catalog) · `lib/montree/phonics/sync-phonics-works.ts` (seeder) · `app/api/montree/features/route.ts` (toggle hook) · `app/api/montree/works/route.ts` (virtual merge removed) · `scripts/{seed,verify}-phonics.ts` · `migrations/2026-06-06_phonics_works_seed.sql`.

## ✅ Dark Phonics content system — LIVE

One hub, six tabs: **montree.xyz/dark-phonics.html**
- 🎵 Songs (49, with audio players + downloads — files uploaded to the `dark-phonics` Supabase bucket)
- 🎤 Sing-Along Cards · 📕 Picture Books · 📗 Easy Readers (decodable, verified) — all with character-led Midjourney prompts + a Step-0 **Character Sheet** prompt per book
- Original Songs / Original Readers (for comparison)

**Midjourney consistency recipe (in the page notes):** make the Character Sheet first → use it in Image Prompts (Omni-Reference) at `--ow 400`+ → keep `--sref` fixed (or one fixed style image) → add a fixed `--seed` → identical character text every page.

**Readers prompt fix (2026-06-06):** the readers' image prompts were rebuilt so each page front-loads its TARGET WORD as the big, concrete subject (vocabulary clarity). Root bug found & fixed: the shared style block ended with "...the main subject large, clear, well-lit and centred", which made MJ re-inflate the cute character on every page and bury the target object (e.g. the hill vanished). Style block is now pure art-style; composition lives only in each page's line. Live on `public/dark-phonics-readers.html`.

## ✅ Security hardening — LIVE & verified

- **Closed a real data leak**: the public anon key could read children/media/schools straight from Supabase. Fixed with RLS default-deny; re-probe confirms 0 leaks. (`scripts/probe-rls.mjs`)
- Login token cut 365d → 30d; CI guards (tenant-scoping, TS-budget); first Vitest tests; `.env.local` confirmed never committed.
- **Your Railway to-dos (still open):** delete `NEXT_PUBLIC_ADMIN_PASSWORD`; strengthen the 6-digit `SUPER_ADMIN_PASSWORD`.

## ⬜ App Store — prepped, not submitted

Pre-flight found 4 rejection blockers. **Done:** iOS permission strings + privacy manifest (branch `appstore/phase1-native`), privacy policy live at **montree.xyz/privacy**. **Still needed:** in-app account deletion (mandatory), demo login for reviewers, Apple+Google accounts, finish the native build. Details: `APPLE_PREFLIGHT_CHECKLIST.md`, `APP_STORE_ROADMAP.md`.

## Open items (priority)

1. Your two Railway security edits (5 min).
2. ~~Verify phonics progress lands per child~~ — done & audited 100% clean (real-rows fix).
3. In-app account deletion (App Store blocker + pilot consent).
4. Inline song player on the phonics work detail.
5. The school pilot + error tracking before public store launch.

## Key references

- Branches: `main` (everything live) · `appstore/phase1-native` (native iOS prep, unmerged) · `feature/phonics-works-pack` (merged).
- Flag: `phonics_works` (per-school, default OFF; ON for Whale Class `c6280fae-567c-45ed-ad4d-934eae79aabc`).
- Detail docs in this folder: APPLE_PREFLIGHT_CHECKLIST · APP_STORE_ROADMAP · PRIVACY_POLICY_DRAFT · HARDENING_STATUS · PHONICS_WORKS_PLAN · WORD_OPTIMIZATION_PASS.
