// Sentence-match generator reuses the canonical CardPreview from card-generator.
// The `label` prop on Card holds a sentence in this context.
export { default } from '../card-generator/CardPreview';
