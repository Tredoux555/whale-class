// Superseded by the generic SecretGate (Jun 14) — the personal platform now has
// TWO secret doors (Diary + Messages), each with its own phrase, both driven by
// SecretGate. Kept as a re-export so any lingering import still resolves.
export { default } from '@/components/story/personal/SecretGate';
