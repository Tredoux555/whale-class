'use client';

// The Birthdays tool uses the same class-emblem plumbing as Tracing Work — a
// teacher's uploaded logo, read straight off the File. Re-exported here so the
// birthdays feature imports stay inside its own module.
export { fileToArrayBuffer, loadDefaultLogo } from '../tracing/assets';
