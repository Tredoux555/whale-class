# Circle-Time Art — Remap Pass (2026-09-09)

Purpose: fix "WRONG" slots for free where the misplaced image is actually the
correct art for a *different* slot (a scripted-download mixup), before
building the fresh generation list (art-todo-2.json).

Method: every candidate was cross-checked against `audit_all.txt` (the
destination slot's *current* real-world state), not just against the
coordinator's example chain — a proposed destination that audit_all.txt
already showed as OK was left alone (no free fix available there; that
"WRONG" source image just gets discarded/regenerated in place instead).

## Applied remaps (10, via `cp`, sources left in place)

| # | Source (had the wrong-but-useful image) | Destination (now fixed) | What it is |
|---|---|---|---|
| 1 | week7/ct-week7-card-apple.jpg | week7/ct-week7-card-banana.jpg | banana |
| 2 | week7/ct-week7-poster-sentence-frames.jpg | week7/ct-week7-card-apple.jpg | apple |
| 3 | week7/ct-week7-sign-eat-less.jpg | week5/ct-week5-card-lemon.jpg | lemon |
| 4 | week7/ct-week7-card-egg.jpg | week5/ct-week5-card-yellow-pear.jpg | yellow pear |
| 5 | week6/ct-week6-card-can-dress.jpg | week6/ct-week6-badge-star.jpg | star/ribbon badge |
| 6 | week6/ct-week6-card-can-rake.jpg | week6/ct-week6-card-tree.jpg | autumn tree |
| 7 | week16/ct-week16-card-can-wrap.jpg | week17/ct-week17-sign-sunny-day-sign.jpg | sunny path/hat/sunglasses |
| 8 | week17/ct-week17-card-cloudy.jpg | week17/ct-week17-card-ice-lolly.jpg | red ice lolly |
| 9 | week19/ct-week19-card-can-count.jpg | week19/ct-week19-card-can-wave.jpg | child waving |
| 10 | week20/ct-week20-card-fu-diamond.jpg | week19/ct-week19-card-rice-bowl.jpg | rice bowl (audit_bad.txt says the fu-diamond slot "shows a bowl of rice, not a fu-character diamond" — exactly the missing week19/card-rice-bowl art) |

All 10 source files were left untouched (not moved) since they will be
regenerated with correct content later; only the destination slots were
overwritten via `cp`.

## Candidates evaluated and rejected (no free fix applied)

- **week7 → week5 "red mitten / red scarf / red flag" chain** (the
  coordinator's example): audit_all.txt shows week5's `card-red-mitten`,
  `card-red-scarf`, `card-red-flag`, `card-persimmon`, `card-corn`,
  `card-sunflower`, `card-banana` and `card-yellow-hat` are **already OK**.
  There is no wrong destination to remap into, so week7's `card-milk`,
  `card-grain`, `card-fruit` and `card-banana` (all WRONG) get no free fix —
  they go straight into art-todo-2.json for regeneration instead.
- **week16 `card-coat` / `card-woolly-hat` swap**: audit_bad.txt shows both
  are WRONG but neither shows the *other's* actual content (coat shows
  "hat, mittens, and scarf"; woolly-hat shows "a full puffer coat with fur
  hood" — a *different* coat depiction, not a match for the coat slot's own
  needed image). Not an unambiguous swap, so both were left as-is and both
  are in the regeneration list (woolly-hat as explicitly required).
- **week16 can-* slots showing week17 weather art** (rain cloud, raindrop,
  puddle, weather-icons row, snow cloud, sunny/rainy signs, window-look):
  checked each corresponding week17 destination in audit_all.txt — all of
  them (`card-cloudy`→now fixed above, `card-can-shiver`, `card-can-run`,
  `card-can-hide`, etc.) were themselves already flagged WRONG or already
  fixed by another remap, except the two applied above (#7, #8). The rest
  had no single clean destination slot that unambiguously matched the
  source description, so no further remap was applied; those week16 can-*
  slots go into the regeneration list as-is.

## Note on the coordinator's suggested chain vs. actual audit data

The coordinator's message described the week7→week5 chain as if the week5
destinations were currently wrong. Cross-checking against audit_all.txt
showed the opposite: those week5 slots are already correct. Per the
instruction to remap only when the description "unambiguously matches the
destination slot name" **and** to check the destination's actual current
state, those items were excluded from the remap list. This is why the
applied list (10 items) is smaller than the coordinator's example implied.
